{% macro pageTitle(pageHeader="", pageSubheader="", margin="") %}
  <div class="subheader">
    <div class="d-flex">
      <h4>{{ pageSubheader }}</h4>
      <div class="flex-grow-1 align-self-center">
        <hr>
      </div>
    </div>
    <h1 class="{{ margin }}">{{ pageHeader }}</h1>
  </div>
{% endmacro %}